2nd JavaScript hand in.

Current bugs:
- When adding a note, the cursor positions itself to the end of the form instead of the beginning, meaning it focuses on the "note" field instead of "title" field. 